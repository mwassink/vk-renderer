
#include "../device.h"


int CALLBACK WinMain(HINSTANCE hInstance,
                     HINSTANCE prevInstance,
                     LPSTR commandLine,
                     int showCode) {
    VulkanContext ctx;
    ctx.Init();
    
}

